spring.application.name=urlShortener-sb

spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

# This below property will help to update the databse if anything is changed
spring.jpa.hibernate.ddl-auto=update

# This below property will show the sql queries that need to be used to crate things i database this is not mandatory
spring.jpa.show-sql=true

# This tells hibernate or JPA to generate the SQL Queries according to the MySQL Database.
spring.jpa.properties.hibernate.dialect=${DATABASE_DIALECT}

jwt.secret=${JWT_SECRET}
jwt.expiration=172800000


logging.level.org.springframework.security=DEBUG
logging.level.org.url.shortener=DEBUG


frontend.url=${FRONTEND_URL}


spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=urlshortener12@gmail.com
spring.mail.password=aubhyypgshmfaien
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
